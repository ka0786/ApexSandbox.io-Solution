public static String reverseWords(String str){
    if(str == null) return str;
    String newStr;
    for(String st : str.split(' ')){
        newStr += (st.reverse() + ' ');
    }

    return newStr.trim();
}